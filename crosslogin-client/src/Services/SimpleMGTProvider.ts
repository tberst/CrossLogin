export {}
// import { IProvider } from '@microsoft/mgt-element';

// import { Providers, SimpleProvider, ProviderState } from '@microsoft/mgt-element';

// import MsalClient from './MsalClient';

// function getAccessToken(scopes: string[]): Promise<string> {
//     return new MsalClient().getAccessToken(scopes);
// }

// async function login() {
//     //login code
//     Providers.globalProvider.setState(ProviderState.SignedIn)
// }

// async function logout() {
//     // logout code
//     Providers.globalProvider.setState(ProviderState.SignedOut);
// }

// export  const SimpleMGTProvider = new SimpleProvider(getAccessToken, login, logout);
